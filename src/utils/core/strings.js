const successServerString = 'Api rest corriendo en el puerto '
const errorConnectionString = 'No se pudo conectar con la base de datos.'

module.exports = {
    successServerString,
    errorConnectionString,
}